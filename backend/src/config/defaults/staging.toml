[server]
host = "0.0.0.0"
port = 8080
max_connections = 5000

[database]
max_connections = 50
min_connections = 5
connect_timeout_secs = 10
idle_timeout_secs = 300

[redis]
pool_size = 50

[observability]
log_level = "debug"
enable_metrics = true
